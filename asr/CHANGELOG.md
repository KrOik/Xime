# Changelog

## 1.0.0

- Installable `doutype` package with service layer (`AsrService`, `NlpService`, `LiveAsrService`)
- HTTP API via `create_app` / `doutype serve`
- CLI: `doctor`, `transcribe`, `serve`, `ui`
- User documentation under `docs/`
- Historical workspace materials moved to `archive/`
